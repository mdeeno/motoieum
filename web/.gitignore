# 1. 시스템 파일 (운영체제)
.DS_Store
._*

# 2. 의존성 모듈 (용량 큼)
node_modules
web/node_modules

# 3. Next.js 빌드 결과물 (여기가 핵심!)
.next
web/.next
out
build

# 4. 환경 변수 (보안 중요)
.env
.env.local
.env.*.local
web/.env
web/.env.local
.vercel
